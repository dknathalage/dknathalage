version: "3"

tasks:
  test.setup:
    desc: Setup
    cmds:
      - mkdir -p ./tools
      - go install github.com/mfridman/tparse@latest
      - mv $(go env GOPATH)/bin/tparse ./tools

  test.unit:
    desc: Run unit tests
    cmds:
      - go test -coverprofile=coverage.txt -covermode=atomic ./... | tparse -all -notests -format markdown >> test_report.md
    env:
      GOFLAGS: "-v -count=1 -json"

  lint.golangci:
    desc: Run golangci-lint
    cmds:
      - $(go env GOPATH)/bin/golangci-lint run --timeout 2m
