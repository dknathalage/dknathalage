# Auto generated. Do not edit.

name: Build {{ .Name }} 🏗️

on:
  push:
{{ if .Source.Paths }}
    paths:
{{- range .Source.Paths }}
      - {{ . }}
{{- end }}{{- end }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4