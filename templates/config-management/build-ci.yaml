# Auto generated. Do not edit.

name: Build {{ .Name }} 🏗️

on:
  push:
    paths:
      {{- range .Source.Paths }}
      - {{ . }}
      {{- end }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4