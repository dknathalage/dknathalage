# Auto generated. Do not edit.

name: Build invoice 🏗️

on:
  push:
    paths:
      - cmd/invoice

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
			- name: Checkout
				uses: actions/checkout@v4