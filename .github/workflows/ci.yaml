name: CI Workflow ⚙️

permissions:
  contents: read
  actions: read
  pull-requests: write

on:
  push:
    branches:
      - main

  release:
    types:
      - published
jobs:
  quality:
    uses: ./.github/workflows/go-quality.yaml
