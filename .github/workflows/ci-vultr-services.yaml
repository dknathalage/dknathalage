on:
  push:
    branches:
      - main

permissions:
  contents: write

env:
  WORKFLOW_ROOT: dknathalage/dknathalage/.github/workflows

jobs:
  quality-check:
    runs-on: ubuntu-latest
    steps:
      - uses: ${{ env.WORKFLOW_ROOT }}/ci-quality.yaml@main
        with:
          service: test

  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: ${{ env.WORKFLOW_ROOT }}/cd-terraform.yaml@main

  docker-build:
    needs: quality-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ${{ env.WORKFLOW_ROOT }}/ci-docker.yaml@main
        with:
          service: test
          dockerfile: ./cmd/data/Dockerfile

  vultr-services:
    needs: [terraform, docker-build]
    runs-on: ubuntu-latest
    steps:
      - uses: ${{ env.WORKFLOW_ROOT }}/cd-vultr.yaml@main
        with:
          service: test
          server_ip: 67.219.103.200

  